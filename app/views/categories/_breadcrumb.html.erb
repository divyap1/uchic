<div class="col-md-12">
    <ul class="breadcrumb">
        <% @ancestry.each do |category| %>
        <li><%= link_to category.name, category_path(category.id) %></li>
        <%end%>
        <li><%= @category.name %></li>
    </ul>
</div>
